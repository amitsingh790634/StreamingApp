apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.frontend.serviceName }}
  labels:
    {{- include "streamingapp.componentLabels" (dict "root" . "component" .Values.services.frontend.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "streamingapp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.services.frontend.name }}
  ports:
    - name: http
      port: {{ .Values.services.frontend.port }}
      targetPort: http
