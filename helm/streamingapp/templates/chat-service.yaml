apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.chat.serviceName }}
  labels:
    {{- include "streamingapp.componentLabels" (dict "root" . "component" .Values.services.chat.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "streamingapp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.services.chat.name }}
  ports:
    - name: http
      port: {{ .Values.services.chat.port }}
      targetPort: http
