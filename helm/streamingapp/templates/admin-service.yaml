apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.admin.serviceName }}
  labels:
    {{- include "streamingapp.componentLabels" (dict "root" . "component" .Values.services.admin.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "streamingapp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: {{ .Values.services.admin.name }}
  ports:
    - name: http
      port: {{ .Values.services.admin.port }}
      targetPort: http
